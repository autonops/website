import { ToolPageWrapper } from '@/components/dashboard/tool-page-wrapper'

export const metadata = {
  title: 'Tessera - Monolith Decomposition',
}

export default function TesseraPage() {
  return <ToolPageWrapper toolId="tessera" />
}
